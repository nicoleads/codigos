#include <stdio.h>

int main() {
    int cont, soma;

    soma=0;

    for(cont=1; cont<=100; cont++){

        soma= soma+cont;
        }

    printf("A soma de 1 a 100 e: %d ", soma);

    return 0;
}
