#include <stdio.h>

int main () {
    int numeros [] = {1, 2, 3, 4, 5}; // simplifica as variaveis

    //exibe qual elemento do array deve exibir
    printf("Elementos no indice 4: %d\n", numeros [4]);

    return 0;
}
